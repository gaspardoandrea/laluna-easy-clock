<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EasyToReadClock" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.EasyToReadClock.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:actionMenuTextColor" tools:targetApi="l">@color/cardview_dark_background</item>
        <item name="android:textColor" tools:targetApi="l">@color/cardview_dark_background</item>
        <item name="android:titleTextColor" tools:targetApi="l">@color/cardview_dark_background</item>
        <item name="android:subtitleTextColor" tools:targetApi="l">@color/cardview_dark_background</item>
    </style>

    <style name="Theme.EasyToReadClock.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EasyToReadClock.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:actionMenuTextColor" tools:targetApi="l">@color/cardview_dark_background</item>
    </style>

</resources>